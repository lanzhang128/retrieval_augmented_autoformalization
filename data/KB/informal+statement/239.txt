{"informal": " Assuming `n` and `k` are natural numbers, and `a` is a function from the first `n` natural numbers to set `X`, while `b` is a function from the first `k+1` natural numbers to `X`, then the concatenation of the initial `k` elements of `b` with `a` is equal to the concatenation of `a` and `b`.", "statement": "lemma concat_init_last_elem: \n  assumes \"n \\<in> nat\"  \"k \\<in> nat\" and \n  \"a: n \\<rightarrow> X\"  and \"b : succ(k) \\<rightarrow> X\"\n  shows \"Append(Concat(a,Init(b)),b`(k)) = Concat(a,b)\"\n  "}