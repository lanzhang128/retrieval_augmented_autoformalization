{"informal": " Given a natural number `n` and a sequence `a` of length `n+1` over domain `X`, the tail of `a` is equal to the set of pairs `<k, a!(k+1)>` for `k` in `{0..n}`. \n\nInformally: \n\nGiven a sequence `a` of length `n+1` over domain `X`, the tail of `a` is the subset of pairs `<k, a!(k+1)>` for index `k` in the set `{0, 1, ..., n}`.", "statement": "lemma tail_as_set: assumes \"n \\<in> nat\" and \"a: n #+ 1 \\<rightarrow> X\"\n  shows \"Tail(a) = {\\<langle>k,a`(k #+ 1)\\<rangle>. k\\<in>n}\"\n  "}