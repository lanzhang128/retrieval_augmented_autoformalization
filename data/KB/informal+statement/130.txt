{"informal": " Given an equivalence relation `r` over set `X`, `A` being a subset of `X` that is saturated under `r`, and `x` and `y` being elements of `X` in `A` with `x` not being in `A` if and only if `y` is not, then the sets `r`-class of `x` and `r`-class of `y` belong to the set `B`, and the belonging of `r`-class of `x` to `B` Xor the belonging of `r`-class of `y` to `B` holds.", "statement": "lemma EquivClass_3_L7: assumes \"equiv(X,r)\"\n  and \"IsSaturated(r,A)\" and \"A\\<subseteq>X\"\n  and \"x\\<in>X\"  \"y\\<in>X\"\n  and \"B = {r``{x}. x\\<in>A}\"\n  and \"(x\\<in>A) Xor (y\\<in>A)\"\n  shows \"(r``{x} \\<in> B)  Xor (r``{y} \\<in> B)\"\n  "}