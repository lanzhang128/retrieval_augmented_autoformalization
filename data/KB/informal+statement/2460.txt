{"informal": " If function `f` is an order isomorphism from set `A` to `B` via relation `r` and `r` is a subset of `A` times `A`, then `r` is total on `A`. \n\nIn other words, every element in `A` is related by `r` to exactly one element in `A`.", "statement": "lemma ord_iso_pres_tot: assumes A1: \"f \\<in> ord_iso(A,r,B,R)\" and \n  A2: \"r \\<subseteq> A\\<times>A\" and A3: \"R  {is total on} B\"\n  shows \"r  {is total on} A\"\n"}