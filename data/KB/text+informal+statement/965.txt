{"text": "text\\<open>Induction - on (real) natural numbers -  version for humans.\\<close>\n", "informal": "Assuming `<psi>` is a unary predicate, the statement can be translated into natural language as follows:\n\nIf `<one>` is an element that satisfies `<psi>`, and for all natural numbers `k`, if `k` satisfies `<psi>`, then `k` with `<one>` as its successor also satisfies `<psi>`, and `n` is a natural number, then `n` also satisfies `<psi>`.", "statement": "corollary (in MMIsar0) nnind1: \n  assumes A1: \"\\<psi>(\\<one>)\" and \n  A2: \"\\<forall>k \\<in> \\<nat>. \\<psi>(k) \\<longrightarrow> \\<psi>(k\\<ca>\\<one>)\" and\n  A3: \"n \\<in> \\<nat>\"\n  shows \"\\<psi>(n)\"\n"}