{"text": "text\\<open>If the relation is reflexive, the endpoints belong to the interval.\\<close>\n", "informal": " Given that `X` is a non-empty set, `r` is a reflexive relation on `X`, `a` and `b` are elements in `X`, and `(a, b)` is a pair in `r`, it can be concluded that both `a` and `b` belong to the interval defined by `r` around `a`.\n\nIn simpler terms, if `X` has `a` and `b` as elements and `r` is a reflexive relation among them where `(a, b)` is a pair in `r`, then both `a` and `b` are part of the interval [a, b]_r.", "statement": "lemma Order_ZF_2_L2: assumes \"refl(X,r)\" \n  and \"a\\<in>X\"  \"b\\<in>X\" and \"\\<langle> a,b\\<rangle> \\<in> r\"\n  shows \n  \"a \\<in> Interval(r,a,b)\"  \n  \"b \\<in> Interval(r,a,b)\"  \n  "}