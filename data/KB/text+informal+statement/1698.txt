{"text": "text\\<open>The product of semigroup (actually, magma -- we don't\n   need associativity for this) elements is in the semigroup.\\<close>\n", "informal": " If `n` is a natural number and `a` is a function from `succ(n)` to `G`, then the product `\\<Prod> a` is an element of `G`. \n\nIn simpler terms, if `a` is a function defined on the successor of a natural number `n` and its values belong to some set `G`, then the product of `a` will also be an element of `G`.", "statement": "lemma (in semigr0) prod_type: \n  assumes \"n \\<in> nat\" and \"a : succ(n) \\<rightarrow> G\"\n  shows \"(\\<Prod> a) \\<in> G\"\n"}