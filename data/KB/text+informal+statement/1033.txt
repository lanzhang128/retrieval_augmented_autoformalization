{"text": "text\\<open>Taking one more multiple of $x$ adds $x$. \\<close>\n", "informal": " Given a natural number `n` and an element `x` in a monoid `G`, we have the equalities:\n\n- `(n + 1) * x = n * x + x`\n- `(n + 1) * x = x + n * x`\n\nThis can be read as: For all natural number `n` and element `x` in monoid `G`, the product `(n + 1) * x` equals the sum of the products `n * x` and `x`, and also equals the sum of the products `x` and `n * x`.", "statement": "lemma (in monoid1) nat_mult_add_one: assumes \"n\\<in>nat\" \"x\\<in>G\" \n  shows \"(n #+ 1)\\<cdot>x = n\\<cdot>x \\<oplus> x\" and \"(n #+ 1)\\<cdot>x = x \\<oplus> n\\<cdot>x\"\n"}