{"text": "text\\<open>A base filter for a collection satisfies the filter base condition iff that collection\nis in fact a filter.\\<close>\n", "informal": " If C is a base filter (not the empty filter), then C satisfies the filter base condition if and only if the union of the empty filter and F is a filter. \n\nOr more succinctly: \n\nA base filter C is equivalent to a filter on the empty union with the empty filter if and only if C is not the empty filter.", "statement": "theorem basic_filter:\n  assumes \"C {is a base filter} \\<FF>\"\n  shows \"(C {satisfies the filter base condition}) \\<longleftrightarrow> (\\<FF> {is a filter on} \\<Union>\\<FF>)\"\n"}