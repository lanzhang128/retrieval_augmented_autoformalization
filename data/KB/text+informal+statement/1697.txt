{"text": "text\\<open>Lemma \\<open>concat_init_last_elem\\<close> rewritten\n  in the notation used in the \\<open>semigr0\\<close> context.\\<close>\n", "informal": " If `n` and `k` are natural numbers, `a` is a function from `n` to set `X`, and `b` is a function from `k+1` to `X`, then the concatenation of `a` and the initial segment of `b` is equal to the concatenation of `a` and `b`, where the initial segment of `b` is the function that maps the first `k` elements to the elements of `b` and the rest to the corresponding elements of `a`.", "statement": "lemma (in semigr0) concat_init_last: \n  assumes \"n \\<in> nat\"  \"k \\<in> nat\" and \n  \"a: n \\<rightarrow> X\"  and \"b : succ(k) \\<rightarrow> X\"\n  shows \"(a \\<squnion> Init(b)) \\<hookleftarrow> b`(k) = a \\<squnion> b\"\n  "}