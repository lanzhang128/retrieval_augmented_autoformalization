{"text": "text\\<open>If an element belongs to an equivalence class, then its image\n  under relation is this equivalence class.\\<close>\n", "informal": " If relation `r` makes `A` an equivalence class, and `C` is an element of `A`'s equivalence class, then the equivalence class of `C` (under `r`) is equal to `C`. \n\nIn simpler terms, if `r` is an equivalence relation on set `A` and an element `x` belongs to set `C` which is a subset of `A`'s equivalence class, then the equivalence class of `x` under `r` is equal to `C`.", "statement": "lemma EquivClass_1_L2: \n  assumes A1: \"equiv(A,r)\"  \"C \\<in> A//r\" and A2: \"x\\<in>C\"\n  shows \"r``{x} = C\"\n"}