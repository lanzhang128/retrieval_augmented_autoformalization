{"text": "text\\<open>The image of the set of upper bounds is the set of upper bounds\n  of the image.\\<close>\n", "informal": " If function `f` is an order isomorphism between sets `A` and `B` with respect to relation `r` and subsets `A` and `R`, and `C` is a non-empty subset of `A`, then the infimum of `C` with respect to `r` equals the infimum of `f(C)` with respect to `R`. \n\nIn simpler terms, for any order isomorphism `f` between sets `A` and `B`, if `C` is a non-empty subset of `A`, then the smallest element in `A` with respect to the relation `r`, that lies in `C`, maps to the smallest element in `B` with respect to `R`, that maps to an element in `f(C)`.", "statement": "lemma ord_iso_pres_min_up_bounds: \n  assumes A1: \"f \\<in> ord_iso(A,r,B,R)\" and  A2: \"r \\<subseteq> A\\<times>A\"  \"R \\<subseteq> B\\<times>B\" and \n  A3: \"C\\<subseteq>A\" and A4: \"C\\<noteq>0\"\n  shows \"f``(\\<Inter>a\\<in>C. r``{a}) = (\\<Inter>b\\<in>f``(C). R``{b})\"\n"}